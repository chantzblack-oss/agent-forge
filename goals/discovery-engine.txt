Audit and elevate the project at C:/Users/ChantzBlack/projects/discovery-engine to production-grade quality. It's a research exploration tool (Flask + Anthropic Claude API + streaming SSE) that surfaces surprising scientific findings and lets users dive into rabbit holes. Read every source file before proposing changes.

Architect: Map the full codebase (discovery_engine.py CLI + discovery_web.py Flask app), identify architectural weaknesses, and design fixes. Key concerns: no error handling on API calls, hardcoded API tool versions, no caching, JSON file persistence with no conflict handling. Define clear acceptance criteria for each agent's deliverables.

Backend: Write ACTUAL CODE for: (1) retry logic with exponential backoff for Claude API calls, (2) structured error handling — API failures, malformed JSON from Claude, streaming timeouts, (3) response caching so repeat queries don't re-fetch, (4) input validation and sanitization on all Flask endpoints, (5) logging with Python's logging module, (6) session/compost file safety — atomic writes, size limits, conflict handling. Deliver complete, drop-in code — not suggestions.

Frontend: The frontend is embedded HTML/JS in discovery_web.py. Write ACTUAL CODE for: (1) proper loading states with progress feedback during long API calls, (2) error display when API calls fail instead of silent failure, (3) input length limits and validation on freeform queries, (4) mobile responsiveness audit and fixes, (5) accessibility — add proper form labels, ARIA attributes, keyboard navigation.

Tester: Write a COMPLETE pytest test suite. Cover: (1) extract_json() parsing with valid, malformed, and empty inputs, (2) parse_threads() with various Claude response formats, (3) Flask endpoint tests — /api/discover, /api/rabbit-hole, /api/compost operations, (4) streaming SSE behavior, (5) edge cases — API timeouts, empty responses, max compost/session limits. Every test must be runnable as-is.

CodeReviewer: Review all proposed changes AND existing code. Flag: security vulnerabilities (especially in Flask routes and user input handling), performance bottlenecks, the hardcoded web_search tool version, dead code, violated best practices. For every issue, provide the fixed code.

Deliver production-ready code I can drop into the project. No planning documents, no TODO lists, no descriptions of what you would do. ACTUAL WORKING CODE ONLY.